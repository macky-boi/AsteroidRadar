<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/navigation_graph_id"
    app:startDestination="@id/list">

    <fragment
        android:id="@+id/list"
        android:name="com.example.asteroidradar.list.ListFragment"
        >
<!--        <action-->
<!--            android:id="@+id/action_login_to_welcome"-->
<!--            app:destination="@id/welcome" />-->
        <action
            android:id="@+id/action_list_to_detailFragment"
            app:destination="@id/detailFragment" />
    </fragment>
    <fragment
        android:id="@+id/detailFragment"
        android:name="com.example.asteroidradar.detail.DetailFragment"
        android:label="DetailFragment" >
        <action
            android:id="@+id/action_detailFragment_to_list"
            app:destination="@id/list" />
    </fragment>

</navigation>